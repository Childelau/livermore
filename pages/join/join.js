Page({
  onLoad:function(){
    var pages = getCurrentPages();
    var currentPage = pages[pages.length - 1]
    console.log(currentPage.options.key)
    // 建立云连接，筛选数据

  }














  // ===============
})